# 포켓몬 센터 연세점 (PCY) 홈페이지

연세대학교 포켓몬 동아리 공식 홈페이지. 한국어/영어/일본어 트릴링궐 단일 페이지.

- 사이트: https://iambin2.github.io/pokemon-center-yonsei/
- 배포: `main` 브랜치에 푸시하면 `.github/workflows/static.yml`이 저장소 루트(`path: '.'`)를 그대로 GitHub Pages로 올린다. 1~2분 소요.
- 이 저장소를 관리하는 사람은 **비개발자**다. 설명은 한국어로, 전문용어는 풀어서 쓴다.

## 저장소 구조

| 경로 | 설명 |
|---|---|
| `index.html` | **사이트 전체.** CSS·JS·이미지(base64)가 전부 인라인된 단일 파일. 약 1.6MB |
| `og.png` | 1200×630 링크 공유 카드. `index.html`의 `og:image`가 이 경로를 가리킨다. 지우지 말 것 |
| `.github/workflows/static.yml` | 자동 배포 설정. 건드리지 말 것 |
| `scripts/` | 검증·이미지 처리 도구. 배포에는 영향 없음 |

빌드 도구·프레임워크·패키지 매니저가 없다. 이는 의도된 제약이다 — 비개발자가 파일 하나만 교체해도 배포가 끝나는 운영 모델을 위한 것이다. 번들러나 프레임워크를 도입하지 말 것.

## 절대 깨뜨리면 안 되는 것

### 1. i18n 3중 정합성

문구는 `index.html` 안 `const i18n = { ko:{}, en:{}, ja:{} }` 사전에 있고, 마크업은 `data-i18n="키"`로 참조한다. `setLang(lang)`이 `innerHTML`을 교체한다.

- **세 언어의 키 집합이 완전히 동일**해야 한다 (현재 252개 × 3).
- **HTML에 직접 적힌 기본 텍스트 = `ko` 값**이어야 한다. 어긋나면 국기 버튼을 한 번도 누르지 않은 첫 방문자에게만 다른 문구가 보인다. 실제로 이 버그로 한국어 페이지에 마스코트 타입 칩이 영어로 떠 있었다.
- 사전에만 있고 안 쓰이는 키, HTML에서 쓰는데 사전에 없는 키가 없어야 한다.

문구를 하나라도 건드리면 세 언어를 모두 고친다. 번역이 필요하면 자연스러운 번역을 직접 만들고, 고유명사(포켓몬·인물)는 검색으로 공식 표기를 확인한다.

키 접두사: `nav. hero. about. history. act. sub. ypl. mas. mem. faq. join. contact. foot.`(섹션 문구) · `ed. er. em* ey. eg. ec.`(임원 메타) · `pn.<한글이름>`(인물) · `pk.<한글포켓몬>`(포켓몬) · `a11y.`(보조 문구)

### 2. 이미지 규격

임원 아이콘은 `POKE_GIFS = { "인물명": "data:image/png;base64,..." }` 맵에 있고, 파일 끝 로더가 `.exec-poke[data-poke]`에 `<img>`를 주입한다.

| 용도 | 규격 | 표시 크기 | 처리 |
|---|---|---|---|
| 임원 카드 | 192×192 PNG | 92px 원형 | LANCZOS 축소 + oxipng level 4 (strip safe) |
| 마스코트 | 512×512 PNG | 최대 280px | 무손실 재압축만 (픽셀 보존) |
| 푸터 로고 | 192×192 PNG | 64px | 브랜드 네이비 단색 + 원형 마스크 |

- 표시 크기의 2배가 원본 해상도의 상한이다. 임원 원형을 96px 넘게 키우려면 이미지도 다시 뽑아야 한다.
- **이미 축소된 이미지를 다시 축소·재인코딩하지 말 것.** 교체는 항상 원본 공식 이미지에서 새로 뽑는다. 재가공하면 화질이 계단식으로 떨어진다.
- 같은 포켓몬을 쓰는 인물이 여럿이면 맵에 중복 저장하지 말고 별칭으로 연결한다:
  ```js
  POKE_GIFS["김한빈"] = POKE_GIFS["김태현"];
  ```
- 이미지를 넣기 전 **반드시 해당 임원의 `data-fav` 값과 대조**한다. 어떤 이미지를 누구에게 넣을지 먼저 표로 보여주고 확인받는다.

### 3. 접근성·성능 기준선

수정 후에도 유지되어야 한다.

- 명암비 WCAG AA 4.5:1 — 현재 미달 0곳. `--ink-mute`는 흰 바탕 4.6:1에 맞춘 값이라 더 밝게 하면 깨진다
- `:focus-visible` 아웃라인 / `prefers-reduced-motion` 대응 / `<main>` + 스킵 링크 / 제목 레벨 연속
- 아코디언 `aria-expanded` + `aria-controls` (FAQ 13, 기수 7)
- 모든 `<img>`에 `width`/`height` (레이아웃 시프트 방지)
- 터치 타깃 44px 이상
- **파일 2MB 이하** (현재 1.62MB, gzip 1.13MB) / **3G 인터랙티브 10초 이하** (현재 약 8.1초)

### 4. 외부 의존

웹폰트 세 곳뿐이다 — Google Fonts(Fraunces / Noto Serif KR / Noto Serif JP), jsDelivr(Pretendard **동적 서브셋**). Pretendard는 반드시 동적 서브셋 URL을 유지한다. 정적 빌드로 되돌리면 한글 전체를 담은 통짜 woff2를 굵기별로 내려받게 된다.

## CSS 구조

원본 스타일시트 뒤에 `/* v2 PATCH */` 주석으로 시작하는 오버라이드 블록이 번호순(1~17)으로 붙어 있다. 개선 사항은 원본 규칙을 직접 고치지 않고 뒤쪽 블록에서 덮어쓰는 방식으로 쌓았다. **새 수정도 같은 방식으로 뒤에 번호를 이어 붙인다.**

토큰: `--navy-deep/--navy/--navy-light`(브랜드) · `--ink/--ink-soft/--ink-mute`(본문) · `--on-navy-mute`(네이비 위 보조) · `--w-wide/--w-mid/--w-read`(1280/1100/920px 컨테이너) · `--gutter` · `--section-pad`

**레이아웃 규칙**: 한 섹션 안에서 제목·설명·본문 콘텐츠는 반드시 같은 폭 토큰을 쓴다. 서로 다르면 헤더와 본문의 좌우 끝선이 어긋난다.

## 작업 흐름

1. 작업 전 `git pull` — 웹 UI나 GitHub Desktop에서 올린 변경이 있을 수 있다
2. `index.html`을 직접 편집한다. 대량 반복 수정은 파이썬 스크립트로 처리하되, **정규식으로 잘라낼 때 앵커가 유일한지 확인할 것**
3. `python3 scripts/check.py` — 정적 검증
4. `node scripts/verify.mjs` — 브라우저 렌더 검증
5. 사용자에게 변경 내용을 설명하고 확인받은 뒤 커밋

### 큰 편집 시 주의

과거에 CSS와 JS에 **동일한 주석 배너**(`CONTACT MODAL`)가 있어서, JS 블록을 지우려던 스크립트가 CSS 쪽을 잡아 파일 절반을 날린 적이 있다. 문자열 인덱스로 구간을 잘라낼 때는:

- 앵커가 파일 전체에서 유일한지 `count()`로 먼저 확인
- 잘라낼 구간의 길이가 예상 범위인지 `assert`
- 편집 전 `cp index.html /tmp/index.backup.html`

## 검증

```bash
python3 scripts/check.py      # i18n 정합성 · PNG 무결성 · 태그 구조 · 크기 예산
node scripts/verify.mjs       # 3개 국어 전환 · 아코디언 · 이미지 로드 · 콘솔 에러 · 명암비 · 모바일 오버플로
```

또는 `npm test`로 둘 다 실행한다.

- `verify.mjs`는 Playwright가 필요하다. 없으면 `npm install`.
- 사용자 환경은 **Windows**다. 파이썬 실행 명령이 `python3`가 아니라 `python`일 수 있으니, `python3`가 없으면 `python`으로 시도할 것. `npm run check`는 두 가지를 자동으로 시도한다.
- 사용자에게 명령어를 안내할 때는 PowerShell 기준으로 쓸 것.

이미지 추가/교체:

```bash
python3 scripts/add_pokemon.py <이미지경로> <임원이름>   # 검증 → 192px 축소 → 무손실 → 맵에 주입
```

## 배포

사용자가 명시적으로 요청할 때만 커밋·푸시한다. 푸시하면 자동 배포되므로 **푸시 = 실서비스 반영**이다.

```bash
git add -A && git commit -m "설명" && git push
```

푸시 후 사용자에게 알릴 것: 1~2분 뒤 반영되며 `Ctrl+Shift+R`로 강력 새로고침해야 보인다는 점.

되돌리기: `git log`에서 직전 커밋을 찾아 `git revert <sha>` 후 푸시. 사용자가 웹에서 직접 되돌리는 방법은 `docs/인수인계.md` 참고.

## 참고 문서

- `docs/인수인계.md` — 비개발자용 운영 안내서 (계정 인수, 프롬프트 모음, 학기별 할 일, 롤백)
