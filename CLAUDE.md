# 포켓몬 센터 연세점 (PCY) 홈페이지

연세대학교 포켓몬 동아리 공식 홈페이지. 한국어/영어/일본어 3개 국어 단일 페이지, 라이트와 다크 테마.

- 사이트: https://iambin2.github.io/pokemon-center-yonsei/
- 배포: `main` 브랜치에 푸시하면 `.github/workflows/static.yml`이 저장소 루트(`path: '.'`)를 그대로 GitHub Pages로 올린다. 1~2분 소요.
- 이 저장소를 관리하는 사람은 **비개발자**다. 설명은 한국어로, 전문용어는 풀어서 쓴다.

## 저장소 구조

| 경로 | 설명 |
|---|---|
| `index.html` | **사이트 전체.** CSS, JS, 이미지(base64)가 전부 인라인된 단일 파일. 약 1.8MB |
| `og.png` | 1200×630 링크 공유 카드. **첫 화면을 그대로 찍은 것** — 따로 그리지 말고 `node scripts/make-og.mjs`로 다시 만든다. 첫 화면을 고치면 이것도 다시 만들 것 |
| `.github/workflows/static.yml` | 자동 배포 설정. 건드리지 말 것 |
| `scripts/` | 검증·이미지 처리 도구. 배포에는 영향 없음 |

빌드 도구·프레임워크·패키지 매니저가 없다. 이는 의도된 제약이다 — 비개발자가 파일 하나만 교체해도 배포가 끝나는 운영 모델을 위한 것이다. 번들러나 프레임워크를 도입하지 말 것.

## 절대 깨뜨리면 안 되는 것

### 1. i18n 3중 정합성

문구는 `index.html` 안 `const i18n = { ko:{}, en:{}, ja:{} }` 사전에 있고, 마크업은 `data-i18n="키"`로 참조한다. `setLang(lang)`이 `innerHTML`을 교체한다.

- **세 언어의 키 집합이 완전히 동일**해야 한다 (현재 268개 × 3).
- **HTML에 직접 적힌 기본 텍스트 = `ko` 값**이어야 한다. 어긋나면 언어 버튼을 한 번도 누르지 않은 첫 방문자에게만 다른 문구가 보인다. 실제로 이 버그로 한국어 페이지에 마스코트 타입 칩이 영어로 떠 있었다.
- 문구에 가운뎃점(·)을 쓰지 않는다. 나열은 쉼표나 평범한 문장으로 쓴다. (섹션의 영어 라벨은 번역 대상이 아니라 디자인 요소다. 아래 디자인 체계 참고.)
- 일본어는 전각 물음표(？)와 느낌표(！)를 쓰고, 학교 이름은 「延世大学」, 지명은 「シンチョン」처럼 일본어 독자가 읽는 대로 적는다. 영어에는 줄표(—)를 쓰지 않는다.
- 문구를 고친 뒤에는 세 언어를 노트북 화면과 휴대폰 화면에서 모두 확인한다. 마지막 줄에 한두 글자만 남으면 문장을 줄인다.
- 사전에만 있고 안 쓰이는 키, HTML에서 쓰는데 사전에 없는 키가 없어야 한다.

문구를 하나라도 건드리면 세 언어를 모두 고친다. 번역이 필요하면 자연스러운 번역을 직접 만들고, 고유명사(포켓몬·인물)는 검색으로 공식 표기를 확인한다.

키 접두사: `meta. brand. nav. hero. about. history. act. sub. ypl. mas. mem. faq. join. facts. contact. foot.`(섹션 문구), `ed. er. em* ey. eg. ec.`(임원 메타), `pn.<한글이름>`(인물), `pk.<한글포켓몬>`(포켓몬), `a11y.`(보조 문구). 대체 텍스트용 `data-i18n-aria`(마스코트 그림)와 스크립트에서만 쓰는 키(`contact.copied`)도 있다.

`facts.*`는 가입 섹션 아래 한 줄 요약(회비, 가입 대상, 정기 모임)이다. 회비나 회원 수가 바뀌면 FAQ, 소개 문단과 함께 여기도 고친다.

임원 한 명은 이름(`pn.`), 직책(`er.`), 학과(`em`), 학번(`ey.`), 국적(`nat.`), 최애 포켓몬(`pk.`)으로 이루어진다. 학과 이름은 모두 "~학과"로 쓴다(학부로 쓰지 않는다). 국적은 모두 표시하고, 기본값은 `nat.kr`이다. 국기는 이모지가 아니라 직접 그린 SVG 기호(`#f-kr`, `#f-jp`, `#f-fr`)다. 이모지 국기는 윈도우에서 글자로 보이기 때문이다. 새 국적을 추가하려면 같은 방식으로 `#f-<코드>` 기호를 그리고 `nat.<코드>` 문구를 세 언어로 넣는다(화면 낭독기용으로 이름이 함께 들어간다). 외국인 임원은 이름을 성과 이름 모두 적는다(예: 나카쓰지 아오바 / Aoba Nakatsuji / 中辻 青葉).

### 2. 이미지 규격

임원 아이콘은 `POKE_GIFS = { "인물명": "data:image/png;base64,..." }` 맵에 있고, 파일 끝 로더가 `.exec-poke[data-poke]`에 `<img>`를 주입한다.

| 용도 | 규격 | 표시 크기 | 처리 |
|---|---|---|---|
| 임원 카드 | 160×160 무손실 WebP (전원) | 84px 원형 (그림 78px) | 공식 HOME 렌더에서 LANCZOS 축소 후 무손실 WebP. 표시 78px의 2배에 맞춘 크기다. 2026-09에 192px이던 1~7기 36장을 원본에서 다시 뽑아 160px로 맞췄다 |
| 마스코트 | 512×512 무손실 WebP | 마스코트 칸 260px | 무손실 재압축만 (픽셀 보존, PNG에서 WebP로 형식만 바꿈). CSS `.art-rufflet`, `.art-braviary`에 한 번씩만 들어 있고 두 곳이 같이 쓴다 |
| 동아리 엠블럼 | SVG 기호 `#emblem` (벡터라 해상도 제한 없음) | 상단 메뉴 40px(옆에 워드마크), 바닥글 64px | 2026-09 새 로고(2048px 원본)에서 만듦. 원, 고리, 몬스터볼은 원본에서 잰 반지름으로 그리고, 글자와 포켓몬 그림은 원본에서 윤곽을 땄다. 한 번만 들어 있고 두 곳이 `<use href="#emblem">`로 같이 쓴다. 모양을 고치지 말 것 |
| 풀숲 포켓몬 | 수리둥보, 워글은 마스코트 그림을 빌려 씀. 피카츄만 높이 326px WebP | 첫 화면 풀숲, 최대 약 163px | 피카츄는 공식 HOME 렌더(25번)를 줄인 것. PNG는 용량 한도를 넘어 WebP(품질 85)로 넣었다. 첫 화면 풀숲 스크립트 안 `PIKACHU`에 있다 |
| 탭 아이콘 | 32×32 PNG, 홈 화면용 180×180 PNG | 브라우저 탭, 휴대폰 홈 화면 | 같은 로고 원본에서 새로 뽑음. 180은 흰 바탕(아이폰이 투명을 못 씀), 256색 |


- 표시 크기의 2배가 원본 해상도의 상한이다. 임원 원형을 84px 넘게 키우려면 이미지도 다시 뽑아야 한다.
- 임원 아이콘의 원본은 PokeAPI의 공식 HOME 렌더다: `https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/home/<도감번호>.png`. 기존 이미지도 모두 여기서 나왔다(픽셀 단위로 동일함을 확인). 단 대굴레오, 글레이시아, 토게키스, 토오, 어흥염, 메타그로스 6장은 원형 안에 맞게 누군가 크기와 위치를 따로 잡아 두었고, 다시 뽑을 때 같은 구도를 재현했다.
- 이미지는 **무손실 WebP**로 넣는다. 같은 픽셀을 PNG보다 25~30% 작게 담는다. 모든 최신 브라우저가 지원한다.
- **이미 축소된 이미지를 다시 축소·재인코딩하지 말 것.** 교체는 항상 원본 공식 이미지에서 새로 뽑는다. 재가공하면 화질이 계단식으로 떨어진다.
- 같은 포켓몬을 쓰는 인물이 여럿이면 맵에 중복 저장하지 말고 별칭으로 연결한다:
  ```js
  POKE_GIFS["김한빈"] = POKE_GIFS["김태현"];
  ```
- 이미지를 넣기 전 **반드시 해당 임원의 `data-fav` 값과 대조**한다. 어떤 이미지를 누구에게 넣을지 먼저 표로 보여주고 확인받는다.

### 3. 접근성·성능 기준선

수정 후에도 유지되어야 한다.

- 명암비 WCAG AA 4.5:1 — 라이트와 다크 두 테마 모두 미달 0곳. `--ink-mute`(라이트 #666E77, 다크 #8E99A9)가 가장 옅은 글자색이라 더 옅게 하면 깨진다
- 라이트/다크 테마: 기기 설정을 따르고, 상단 해/달 버튼으로 바꾸면 `localStorage`의 `pcy-theme`에 기억한다. `<head>`의 짧은 스크립트가 첫 화면 전에 테마를 정한다
- `:focus-visible` 아웃라인 / `prefers-reduced-motion` 대응 / `<main>` + 스킵 링크 / 제목 레벨 연속
- 아코디언 `aria-expanded` + `aria-controls` (FAQ 13개, 기수 7개), 닫힌 내용에는 `inert`
- 모든 `<img>`에 `width`/`height` (레이아웃 시프트 방지)
- 터치 타깃 44px 이상
- **파일 2MB 이하** (현재 1.25MB, gzip 0.83MB. 파일의 대부분은 임원 아이콘이다. 이 한도는 경고일 뿐 커밋이나 배포를 막지는 않는다) / **3G 인터랙티브 10초 이하** (현재 약 8.1초)

### 4. 외부 의존

웹폰트는 두 곳뿐이다.

- Google Fonts: Fraunces(영문 세리프와 숫자), Noto Serif KR, Noto Serif JP(제목)
- jsDelivr: Pretendard **동적 서브셋** 두 개(본문). 반드시 동적 서브셋 URL을 유지한다. 정적 빌드로 되돌리면 글자 전체를 담은 통짜 woff2를 내려받게 된다.

## 디자인 체계

2026-09 전면 개편. 이전 사이트의 스타일과 배치를 그대로 존중하되, 규칙을 하나로 정리해 새로 만든 판이다. 전체 규칙은 `DESIGN.md`에 있다. 핵심만:

- **글꼴은 두 가지 역할뿐**: 제목, 워드마크, 영어 라벨, 숫자는 세리프(`--serif-kr` Noto Serif KR, `--serif-en` Fraunces, `--serif-ja` Noto Serif JP). 본문과 버튼은 Pretendard(`--sans`). 이 둘 밖의 글꼴을 새로 들이지 말 것.
- **연세 블루(`--navy`, #003876)** 가 제목, 띠(마퀴, 리그, 바닥글), 버튼, 표지의 색이다. 본문은 먹색(`--ink`), 보조는 `--ink-soft`, 가장 옅은 글자는 `--ink-mute`까지만.
- **영어 라벨(`.label`)** 은 이 사이트의 표식이다. 항상 포켓볼 기호(`#i-ball`)와 함께 쓰고, 글자는 대문자에 자간을 넓게 준다. 한국어로 번역하지 않는다.
- **숫자는 기울인 세리프**(`.italic-num`): 활동 카드의 01, 기수의 07, 도감 번호 № 0627.
- **선으로 상자를 두르지 않는다.** 면은 바탕색으로 나누고(`.card`, 모서리 20px), 가는 선은 목록의 줄 사이(연혁, 소모임, 기수, FAQ, 연락처)에만 쓴다. 카드 위쪽 색 띠를 절대 만들지 말 것(이전 사이트의 hover 효과였고, 사용자가 싫어한다).
- 글자 크기(`--t-label`~`--t-quote`), 모서리(`--r-s`, `--r-card`), 움직임(곡선 `--ease` 하나, 길이 `--t1/t2/t3`)은 토큰만 쓴다.
- 다크 테마는 `:root[data-theme="dark"]`에서 토큰만 바꾼다. 색을 직접 적지 말 것.

**레이아웃 규칙**: 모든 섹션은 `.wrap`(최대 1180px) 하나의 폭을 쓴다. 섹션 머리는 `.sec-head`(라벨과 제목이 같은 줄), 리그와 가입 섹션만 가운데 정렬이다.

**임원 목록**은 기수 아코디언(`.exec-gen`)이고 최신 기수가 맨 위에서 열린 상태로 시작한다(현재 8기, 12명). 새 기수를 추가하면 맨 위에 `open` 클래스와 `aria-expanded="true"`로 넣고, 직전 기수는 닫은 뒤 패널에 `inert`를 붙인다. 기수 표시는 기수 이름 하나뿐이고 번호를 따로 붙이지 않는다.

## 작업 흐름

1. 작업 전 `git pull` — 웹 UI나 GitHub Desktop에서 올린 변경이 있을 수 있다
2. `index.html`을 직접 편집한다. 대량 반복 수정은 파이썬 스크립트로 처리하되, **정규식으로 잘라낼 때 앵커가 유일한지 확인할 것**
3. `python3 scripts/check.py` — 정적 검증
4. `node scripts/verify.mjs` — 브라우저 렌더 검증
5. 사용자에게 변경 내용을 설명하고 확인받은 뒤 커밋

### 큰 편집 시 주의

`<script>` 태그는 세 개다: `<head>`의 테마 결정, 본문 끝의 i18n 및 동작, 마지막의 임원 이미지(`POKE_GIFS`). `scripts/check.py`가 이 개수를 확인한다.

과거에 CSS와 JS에 **동일한 주석 배너**(`CONTACT MODAL`)가 있어서, JS 블록을 지우려던 스크립트가 CSS 쪽을 잡아 파일 절반을 날린 적이 있다. 문자열 인덱스로 구간을 잘라낼 때는:

- 앵커가 파일 전체에서 유일한지 `count()`로 먼저 확인
- 잘라낼 구간의 길이가 예상 범위인지 `assert`
- 편집 전 `cp index.html /tmp/index.backup.html`

## 검증

```bash
python3 scripts/check.py      # i18n 정합성 · PNG 무결성 · 태그 구조 · 크기 예산
node scripts/verify.mjs       # 3개 국어 전환 · 아코디언 · 이미지 로드 · 콘솔 에러 · 명암비 · 모바일 오버플로
```

또는 `npm test`로 둘 다 실행한다.

- `verify.mjs`는 Playwright가 필요하다. 없으면 `npm install`.
- 사용자 환경은 **Windows**다. 파이썬 실행 명령이 `python3`가 아니라 `python`일 수 있으니, `python3`가 없으면 `python`으로 시도할 것. `npm run check`는 두 가지를 자동으로 시도한다.
- 사용자에게 명령어를 안내할 때는 PowerShell 기준으로 쓸 것.

이미지 추가/교체:

```bash
python3 scripts/add_pokemon.py <이미지경로> <임원이름>   # 검증 → 160px 축소 → 무손실 WebP → 맵에 주입
```

## 배포

사용자가 명시적으로 요청할 때만 커밋·푸시한다. 푸시하면 자동 배포되므로 **푸시 = 실서비스 반영**이다.

```bash
git add -A && git commit -m "설명" && git push
```

푸시 후 사용자에게 알릴 것: 1~2분 뒤 반영되며 `Ctrl+Shift+R`로 강력 새로고침해야 보인다는 점.

되돌리기: `git log`에서 직전 커밋을 찾아 `git revert <sha>` 후 푸시. 사용자가 웹에서 직접 되돌리는 방법은 `docs/인수인계.md` 참고.

## 참고 문서

- `docs/인수인계.md` — 비개발자용 운영 안내서 (계정 인수, 프롬프트 모음, 학기별 할 일, 롤백)
